import { ContentTable } from '../AdminWidgets.jsx';

// Ruta /admin/contenidos/noticias — antes la sección "news" del admin (Fase 1).
export function NoticiasPage() {
  return <ContentTable section="news" />;
}

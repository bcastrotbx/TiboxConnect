import { ContentTable } from '../AdminWidgets.jsx';

// Ruta /admin/eventos — antes la sección "events" del admin (Fase 1).
export function EventosPage() {
  return <ContentTable section="events" />;
}

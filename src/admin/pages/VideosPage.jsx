import { ContentTable } from '../AdminWidgets.jsx';

// Ruta /admin/contenidos (índice) — antes la sección "videos" del admin (Fase 1).
export function VideosPage() {
  return <ContentTable section="videos" />;
}

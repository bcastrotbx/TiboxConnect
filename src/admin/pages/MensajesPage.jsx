import { MessagesTable } from '../AdminWidgets.jsx';

// Ruta /admin/mensajes (índice) — antes la sección "messages" del admin (Fase 1).
export function MensajesPage() {
  return <MessagesTable />;
}

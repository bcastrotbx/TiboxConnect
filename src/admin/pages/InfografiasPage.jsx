import { ContentTable } from '../AdminWidgets.jsx';

// Ruta /admin/contenidos/infografias — antes la sección "infographics" del admin (Fase 1).
export function InfografiasPage() {
  return <ContentTable section="infographics" />;
}
